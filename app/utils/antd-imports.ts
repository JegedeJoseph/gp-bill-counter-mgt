// Import only the components we need
export { Table, Button, Input, Space, Modal, Form, message, Popconfirm, Tag, Card, Descriptions, Divider } from 'antd';

// Import only the icons we need
export { 
  SearchOutlined, 
  PlusOutlined, 
  EditOutlined, 
  DeleteOutlined, 
  EyeOutlined 
} from '@ant-design/icons';